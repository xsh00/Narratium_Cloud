import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.narratium.app',
  appName: 'Narratium',
  webDir: 'out',
  server: {
    url: 'https://vip1.sillytarven.top/'
  }
};

export default config;
